# --------------------------
# ORACLE → SOLACE CDC CONNECTOR
# --------------------------

spring:
  cloud:
    stream:
      bindings:
        input-0:
          binder: cdcsupplier
        output-0:
          destination: oracle/cdc/fallback
          binder: solace
solace:
  connector:
    workflows:
      0:
        enabled: true
  java:
    host: "tcps://mr-connection-c9cn9q3pajz.messaging.solace.cloud:55443"
    msg-vpn: "gcp-broker"
    client-username: "solace-cloud-client"
    client-password: "2sb0pgtkqenrd6mtjrdljoo7nj"
    trust-store: "/app/config/client-truststore.jks"
    trust-store-password: "changeit"

management:
  metrics:
    export:
      simple:
        enabled: true
  endpoints:
    web:
      exposure:
        include: "health,metrics,loggers,logfile,channels,env,workflows,leaderelection,bindings"

logging:
  level:
    root: INFO
    com.solace.connectors.debezium: INFO
    com.solace.connector.core: INFO
    com.solace.spring.cloud.stream.binder: INFO
cdc:
  stream:
    retryDelayOnException: 2000 #milliseconds to wait before next retry
    retriesOnException: 3
    failIfDynamicDestinationMismatch: true # true -> will stop the connector in case of wrong column name configuration in dynamic destination, false -> fallback to default destination if configured dynamicDestination fails due to wrong column name configuration
    dynamicDestination:  "*:oracle/{tablename}/{operation}"
  schema: false
  flattening:
    enabled: false
debezium:
  properties:
    # max:
    #   batch:
    #     size: 1
    #   queue:
    #     size: 3
    include:
      schema:
        changes: false
    topic:
      prefix: "solace_cdc"
    decimal:
      handling:
        mode: string
    database:
      connection:
        adapter: logminer
      oracle:
        version: 12+
      user: C##DBZUSER
      server:
        id: hibankcdc
        name: hibankcdcd1
      password: strongPassw0rd!
      dbname: FREE
      port: 1521
      hostname: oracle-free
      allowPublicKeyRetrieval: 'true'
    table:
      include:
        list: customers
    connector:
      class: io.debezium.connector.oracle.OracleConnector
    name: oracle-connector
